[**WP Kernel API v0.11.0**](../README.md)

---

[WP Kernel API](../README.md) / createMemoryReporter

# Function: createMemoryReporter()

```ts
function createMemoryReporter(namespace): MemoryReporter;
```

Creates a `MemoryReporter` instance.

## Parameters

### namespace

`string` = `'tests'`

The namespace for the reporter. Defaults to 'tests'.

## Returns

[`MemoryReporter`](../interfaces/MemoryReporter.md)

A `MemoryReporter` instance.
